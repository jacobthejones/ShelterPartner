## Running Integration Tests

To run the integration tests, use the following command:

```
flutter drive --driver=test_driver/integration_test.dart --target=integration_test/dog_workflow_test.dart -d chrome
```
